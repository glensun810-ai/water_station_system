# 灰度环境一键部署命令

## 使用说明

本文档包含完整的灰度环境一键部署命令，适用于阿里云服务器。

## 服务器信息

- **服务器IP**: 120.76.156.83
- **域名**: jhw-ai.com
- **灰度域名**: canary.jhw-ai.com
- **生产环境端口**: 8000
- **灰度环境端口**: 8001

## 部署方式

### 方式1：通过阿里云控制台 Workbench（推荐）

#### 步骤1：打开控制台链接
```
https://swasnext.console.aliyun.com/servers/cn-shenzhen/i-bp1a5d5d1x2j3j3j3j3j3
```

#### 步骤2：输入密码登录
- 第一个密码（控制台）: `sgl@810SGl`
- 第二个密码（服务器）: `sgl@810jhw`

#### 步骤3：点击 "远程连接" → "通过Workbench远程连接"

#### 步骤4：复制并粘贴以下一键部署命令

```bash
systemctl start sshd nginx && systemctl enable sshd nginx && cd /var/www/jhw-ai.com/backend && source venv/bin/activate && pkill -f uvicorn || true && nohup python -m uvicorn main:app --host 127.0.0.1 --port 8000 > /var/log/water-api.log 2>&1 & mkdir -p /var/www/canary-jhw-ai.com/{backend,water,water-admin} && cp /var/www/jhw-ai.com/backend/waterms.db /var/www/canary-jhw-ai.com/backend/waterms_canary.db && rsync -av --exclude='*.db' --exclude='*.sqlite' --exclude='__pycache__' --exclude='venv' /var/www/jhw-ai.com/backend/ /var/www/canary-jhw-ai.com/backend/ && cd /var/www/canary-jhw-ai.com/backend && sed -i 's/waterms\.db/waterms_canary.db/g' main.py && source venv/bin/activate && nohup python -m uvicorn main:app --host 127.0.0.1 --port 8001 > /var/log/water-canary-api.log 2>&1 & cp -r /var/www/jhw-ai.com/water/* /var/www/canary-jhw-ai.com/water/ && cp -r /var/www/jhw-ai.com/water-admin/* /var/www/canary-jhw-ai.com/water-admin/ && cat > /etc/nginx/conf.d/canary.conf << 'EOF'
server {
    listen 8001;
    location /water/ {
        alias /var/www/canary-jhw-ai.com/water/;
        index index.html;
    }
    location /water-admin/ {
        alias /var/www/canary-jhw-ai.com/water-admin/;
        index admin.html;
    }
    location /api/ {
        proxy_pass http://127.0.0.1:8001/api/;
    }
}
EOF
nginx -s reload && echo "✅ 部署完成！访问：http://120.76.156.83/water/ (生产) 和 http://120.76.156.83:8001/water/ (灰度)"
```

### 方式2：通过VNC远程连接

#### 步骤1：登录阿里云控制台
```
https://ecs.console.aliyun.com
```

#### 步骤2：找到服务器实例
搜索IP: `120.76.156.83`

#### 步骤3：VNC远程连接
点击实例ID → 远程连接 → VNC远程连接

#### 步骤4：输入密码
密码: `sgl@810jhw`

#### 步骤5：粘贴执行部署命令
使用上面的一键部署命令

## 部署完成后的访问链接

### 生产环境
- **用户端**: http://120.76.156.83/water/
- **管理后台**: http://120.76.156.83/water-admin/
- **API**: http://120.76.156.83/api/

### 灰度环境
- **用户端**: http://120.76.156.83:8001/water/
- **管理后台**: http://120.76.156.83:8001/water-admin/
- **API**: http://120.76.156.83:8001/api/

## 部署内容说明

### 一键部署命令执行的操作

1. **启动基础服务**
   - 启动并启用 SSH 服务
   - 启动并启用 Nginx 服务

2. **部署生产环境**
   - 启动后端 API 服务（端口 8000）
   - 使用生产数据库 waterms.db

3. **创建灰度环境**
   - 创建灰度环境目录结构
   - 复制生产数据库到灰度数据库（waterms_canary.db）
   - 复制后端代码到灰度环境
   - 修改灰度数据库配置
   - 启动灰度后端服务（端口 8001）

4. **部署前端文件**
   - 复制用户端前端到灰度环境
   - 复制管理后台前端到灰度环境

5. **配置 Nginx**
   - 配置灰度环境 Nginx（端口 8001）
   - 重新加载 Nginx 配置

## 验证部署

### 检查服务状态
```bash
# 检查后端进程
ps aux | grep uvicorn

# 检查端口监听
netstat -tlnp | grep -E "8000|8001|80"

# 检查日志
tail -f /var/log/water-api.log        # 生产环境日志
tail -f /var/log/water-canary-api.log  # 灰度环境日志
```

### 测试 API 健康检查
```bash
# 生产环境
curl http://127.0.0.1:8000/api/health

# 灰度环境
curl http://127.0.0.1:8001/api/health
```

## 故障排查

### 问题1：服务无法启动
```bash
# 检查错误日志
tail -100 /var/log/water-api.log

# 检查 Python 环境
cd /var/www/jhw-ai.com/backend
source venv/bin/activate
python -c "import uvicorn; print('OK')"
```

### 问题2：数据库问题
```bash
# 检查数据库文件
ls -lh /var/www/jhw-ai.com/backend/waterms.db
ls -lh /var/www/canary-jhw-ai.com/backend/waterms_canary.db

# 验证数据库
sqlite3 /var/www/jhw-ai.com/backend/waterms.db "SELECT COUNT(*) FROM users;"
```

### 问题3：Nginx 配置问题
```bash
# 测试 Nginx 配置
nginx -t

# 查看 Nginx 错误日志
tail -50 /var/log/nginx/error.log
```

## 回滚操作

如果灰度环境有问题，可以快速回滚：

```bash
# 停止灰度环境
pkill -f "uvicorn.*8001"

# 或者重新部署
# 重新执行一键部署命令即可
```

## 清理灰度环境

如果需要完全清理灰度环境：

```bash
# 停止灰度服务
pkill -f "uvicorn.*8001"

# 删除灰度目录
rm -rf /var/www/canary-jhw-ai.com

# 删除灰度 Nginx 配置
rm -f /etc/nginx/conf.d/canary.conf
nginx -s reload
```

## 注意事项

1. **数据安全**: 灰度环境使用独立的数据库副本，不会影响生产数据
2. **端口隔离**: 生产环境（8000）和灰度环境（8001）使用不同端口
3. **独立访问**: 灰度环境使用端口8001访问，不影响生产环境
4. **快速回滚**: 如有问题，可立即停止灰度环境

## 预计部署时间

- VNC/Workbench连接: 30秒
- 执行部署命令: 1-2分钟
- 验证访问: 30秒
- **总计**: 约3分钟

## 后续操作

### DNS 配置（可选）

如需使用域名访问灰度环境，需配置DNS解析：

```
记录类型: A记录
主机记录: canary
记录值: 120.76.156.83
TTL: 600
```

配置后可通过域名访问：
- 灰度用户端: http://canary.jhw-ai.com:8001/water/
- 灰度管理后台: http://canary.jhw-ai.com:8001/water-admin/

### SSL 证书配置（可选）

如需HTTPS访问，需要：
1. 申请通配符证书 `*.jhw-ai.com`
2. 或为 `canary.jhw-ai.com` 单独申请证书
3. 配置 Nginx SSL

---

**最后更新**: 2026-04-04
**维护者**: AI产业集群项目组