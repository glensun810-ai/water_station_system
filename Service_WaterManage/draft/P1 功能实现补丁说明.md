# P1 阶段功能实现补丁说明

**日期：** 2026-03-24  
**状态：** 部分完成

---

## 一、已完成功能

### 1. 后端 API（✅ 完成）

已在 `backend/main.py` 中添加以下 API：

```python
# 回收站 API
GET  /api/admin/trash              # 获取回收站记录
POST /api/admin/trash/restore      # 恢复回收站记录
DELETE /api/admin/trash/{id}       # 永久删除回收站记录
```

### 2. 前端回收站页面（✅ 完成）

已在 `frontend/admin.html` 中添加：
- 回收站导航按钮
- 回收站页面 UI
- 回收站相关状态变量

---

## 二、待完成功能

### 1. 结算预览功能

**需要添加的代码位置：** `frontend/admin.html`

**添加结算预览弹窗：**
```html
<!-- 结算预览弹窗 -->
<div v-if="showSettlementPreview" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl p-6 max-w-lg w-full">
        <h3 class="text-lg font-bold mb-4">📋 批量结算预览</h3>
        
        <div class="bg-slate-50 rounded-xl p-4 mb-4">
            <div class="grid grid-cols-2 gap-3">
                <div>
                    <p class="text-xs text-slate-500">选中记录数</p>
                    <p class="text-xl font-bold">{{ selectedTransactions.length }} 条</p>
                </div>
                <div>
                    <p class="text-xs text-slate-500">总结算金额</p>
                    <p class="text-xl font-bold text-blue-600">¥{{ calculateTotalAmount.toFixed(2) }}</p>
                </div>
            </div>
        </div>
        
        <div class="max-h-64 overflow-y-auto mb-4">
            <div v-for="t in selectedTransactions" :key="t.id" 
                 class="flex justify-between items-center py-2 border-b">
                <div>
                    <p class="font-medium">{{ t.user_name }} ({{ t.department }})</p>
                    <p class="text-xs text-slate-500">{{ t.product_name }} × {{ t.quantity }}</p>
                </div>
                <p class="font-bold text-blue-600">¥{{ t.actual_price.toFixed(2) }}</p>
            </div>
        </div>
        
        <div class="mb-4">
            <p class="text-sm font-medium mb-2">结算方式</p>
            <div class="grid grid-cols-4 gap-2">
                <button @click="settlementMethod = 'cash'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2',
                        settlementMethod === 'cash' ? 'border-blue-500 bg-blue-50' : 'border-slate-200']">
                    💵 现金
                </button>
                <button @click="settlementMethod = 'wechat'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2',
                        settlementMethod === 'wechat' ? 'border-green-500 bg-green-50' : 'border-slate-200']">
                    💚 微信
                </button>
                <button @click="settlementMethod = 'alipay'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2',
                        settlementMethod === 'alipay' ? 'border-blue-400 bg-blue-50' : 'border-slate-200']">
                    💙 支付宝
                </button>
                <button @click="settlementMethod = 'transfer'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2',
                        settlementMethod === 'transfer' ? 'border-purple-500 bg-purple-50' : 'border-slate-200']">
                    🏦 对公
                </button>
            </div>
        </div>
        
        <div class="flex gap-3">
            <button @click="showSettlementPreview = false" 
                class="flex-1 px-4 py-2.5 border border-slate-300 rounded-lg font-medium">
                取消
            </button>
            <button @click="confirmSettlementWithPreview" 
                class="flex-1 px-4 py-2.5 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700">
                确认结算
            </button>
        </div>
    </div>
</div>
```

**添加 JavaScript 状态：**
```javascript
const showSettlementPreview = ref(false);
const settlementMethod = ref('cash');

const calculateTotalAmount = computed(() => {
    return selectedTransactions.value.reduce((sum, t) => sum + t.actual_price, 0);
});

const confirmSettlementWithPreview = async () => {
    // 调用结算 API
    // ...
};
```

### 2. 高级筛选功能

**需要添加的代码位置：** `frontend/admin.html`

**添加高级筛选面板：**
```html
<!-- 高级筛选按钮 -->
<button @click="showFilterPanel = true" 
    class="flex items-center gap-2 px-4 py-2 bg-slate-100 rounded-lg hover:bg-slate-200">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
              d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"/>
    </svg>
    高级筛选
</button>

<!-- 高级筛选弹窗 -->
<div v-if="showFilterPanel" class="fixed inset-0 bg-black bg-opacity-50 z-50">
    <!-- 筛选表单... -->
</div>
```

### 3. 全局快捷键

**需要添加的代码位置：** `frontend/admin.html` 的 mounted 钩子

```javascript
// 快捷键监听
const setupKeyboardShortcuts = () => {
    document.addEventListener('keydown', (e) => {
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
            return;
        }
        
        if (e.key === 'g' || e.key === 'G') {
            let nextKey = '';
            const handleNextKey = (event) => {
                nextKey = event.key.toLowerCase();
                if (nextKey === 'd') {
                    activeTab.value = 'dashboard';
                    showToast('已跳转到数据看板', 'success');
                } else if (nextKey === 't') {
                    activeTab.value = 'transactions';
                    showToast('已跳转到交易记录', 'success');
                } else if (nextKey === 'i') {
                    activeTab.value = 'inventory';
                    showToast('已跳转到库存管理', 'success');
                }
                document.removeEventListener('keydown', handleNextKey);
            };
            document.addEventListener('keydown', handleNextKey, { once: true });
        }
        
        if (e.key === '?' && !e.metaKey && !e.ctrlKey) {
            e.preventDefault();
            showShortcutsHelp.value = true;
        }
        
        if (e.key === 'Escape') {
            closeAllModals();
        }
    });
};

// 在 mounted 中调用
onMounted(() => {
    setupKeyboardShortcuts();
});
```

---

## 三、使用说明

### 回收站功能使用

1. **查看回收站**
   - 点击底部导航栏的"回收站"按钮
   - 查看最近 30 天删除的交易记录

2. **恢复记录**
   - 单条恢复：点击"恢复"按钮
   - 批量恢复：勾选记录后点击"批量恢复"

3. **永久删除**
   - 单条删除：点击"永久删除"按钮
   - 批量删除：勾选记录后点击"永久删除"
   - 需要二次确认

### 注意事项

1. 回收站记录保留 30 天
2. 超期记录自动永久删除
3. 只有超级管理员才能永久删除
4. 所有管理员都可以恢复记录

---

## 四、后续工作

### P1-2: 搜索与筛选增强
- [ ] 实现高级筛选面板
- [ ] 实现多维度筛选逻辑
- [ ] 添加筛选条件保存功能

### P1-3: 快捷操作优化
- [ ] 实现全局快捷键
- [ ] 添加快捷键帮助弹窗
- [ ] 实现快捷操作入口

---

**实施负责人：** AI 产品经理专家  
**完成度：** 60%
