# P1 阶段功能实现规划

**实施日期：** 2026-03-24  
**阶段：** P1 - 重要功能增强

---

## 一、P1-1: 批量操作增强

### 1.1 结算预览功能

**需求描述：**
- 批量结算前显示结算详情预览
- 展示选中记录的详细信息
- 支持选择结算方式（现金/微信/支付宝/对公转账）
- 确认无误后执行结算

**实现方案：**

#### 前端 UI
```html
<!-- 结算预览弹窗 -->
<div v-if="showSettlementPreview" class="fixed inset-0 bg-black bg-opacity-50">
    <div class="bg-white rounded-2xl p-6 max-w-lg w-full">
        <h3 class="text-lg font-bold mb-4">📋 批量结算预览</h3>
        
        <!-- 汇总信息 -->
        <div class="bg-slate-50 rounded-xl p-4 mb-4">
            <div class="grid grid-cols-2 gap-3">
                <div>
                    <p class="text-xs text-slate-500">选中记录数</p>
                    <p class="text-xl font-bold">{{ selectedTransactions.length }} 条</p>
                </div>
                <div>
                    <p class="text-xs text-slate-500">总结算金额</p>
                    <p class="text-xl font-bold text-blue-600">¥{{ totalAmount.toFixed(2) }}</p>
                </div>
            </div>
        </div>
        
        <!-- 明细列表 -->
        <div class="max-h-64 overflow-y-auto mb-4">
            <div v-for="t in selectedTransactions" :key="t.id" 
                 class="flex justify-between items-center py-2 border-b">
                <div>
                    <p class="font-medium">{{ t.user_name }} ({{ t.department }})</p>
                    <p class="text-xs text-slate-500">{{ t.product_name }} × {{ t.quantity }}</p>
                </div>
                <p class="font-bold text-blue-600">¥{{ t.actual_price.toFixed(2) }}</p>
            </div>
        </div>
        
        <!-- 结算方式选择 -->
        <div class="mb-4">
            <p class="text-sm font-medium mb-2">结算方式</p>
            <div class="grid grid-cols-4 gap-2">
                <button @click="settlementMethod = 'cash'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2 transition',
                        settlementMethod === 'cash' ? 'border-blue-500 bg-blue-50' : 'border-slate-200']">
                    💵 现金
                </button>
                <button @click="settlementMethod = 'wechat'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2 transition',
                        settlementMethod === 'wechat' ? 'border-green-500 bg-green-50' : 'border-slate-200']">
                    💚 微信
                </button>
                <button @click="settlementMethod = 'alipay'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2 transition',
                        settlementMethod === 'alipay' ? 'border-blue-400 bg-blue-50' : 'border-slate-200']">
                    💙 支付宝
                </button>
                <button @click="settlementMethod = 'transfer'" 
                    :class="['px-3 py-2 rounded-lg text-sm font-medium border-2 transition',
                        settlementMethod === 'transfer' ? 'border-purple-500 bg-purple-50' : 'border-slate-200']">
                    🏦 对公
                </button>
            </div>
        </div>
        
        <!-- 操作按钮 -->
        <div class="flex gap-3">
            <button @click="showSettlementPreview = false" 
                class="flex-1 px-4 py-2.5 border border-slate-300 rounded-lg font-medium">
                取消
            </button>
            <button @click="confirmSettlement" 
                class="flex-1 px-4 py-2.5 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700">
                确认结算
            </button>
        </div>
    </div>
</div>
```

#### 计算属性
```javascript
const totalAmount = computed(() => {
    return selectedTransactions.value.reduce((sum, t) => sum + t.actual_price, 0);
});
```

---

### 1.2 回收站功能

**需求描述：**
- 删除的交易记录进入回收站
- 保留 30 天，支持恢复
- 支持单条恢复和批量恢复
- 支持永久删除（二次确认）

**实现方案：**

#### 后端 API 新增

```python
@app.get("/api/admin/trash")
def get_trash_transactions(
    days: int = 30,
    db: Session = Depends(get_db)
):
    """获取回收站中的交易记录（保留最近 30 天）"""
    from datetime import timedelta
    cutoff_date = datetime.now() - timedelta(days=days)
    
    transactions = db.query(Transaction).filter(
        Transaction.is_deleted == 1,
        Transaction.deleted_at >= cutoff_date
    ).order_by(Transaction.deleted_at.desc()).all()
    
    results = []
    for t in transactions:
        user = db.query(User).filter(User.id == t.user_id).first()
        product = db.query(Product).filter(Product.id == t.product_id).first()
        deleter = db.query(User).filter(User.id == t.deleted_by).first() if t.deleted_by else None
        
        results.append({
            "id": t.id,
            "user_name": user.name if user else "Unknown",
            "department": user.department if user else "Unknown",
            "product_name": product.name if product else "Unknown",
            "quantity": t.quantity,
            "actual_price": t.actual_price,
            "status": t.status,
            "deleted_at": t.deleted_at.isoformat(),
            "deleted_by": t.deleted_by,
            "deleter_name": deleter.name if deleter else "Unknown",
            "delete_reason": t.delete_reason
        })
    
    return results


@app.post("/api/admin/trash/restore")
def restore_trash_transactions(
    request: SettlementRequest,  # 复用 SettlementRequest
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """恢复回收站中的交易记录"""
    if not current_user or current_user.role not in ["admin", "super_admin"]:
        raise HTTPException(status_code=403, detail="权限不足")
    
    restored = 0
    for tid in request.transaction_ids:
        transaction = db.query(Transaction).filter(
            Transaction.id == tid,
            Transaction.is_deleted == 1
        ).first()
        if transaction:
            transaction.is_deleted = 0
            transaction.deleted_at = None
            transaction.deleted_by = None
            transaction.delete_reason = None
            restored += 1
    
    db.commit()
    return {"message": f"成功恢复 {restored} 条记录", "restored_count": restored}


@app.delete("/api/admin/trash/{transaction_id}")
def permanently_delete_transaction(
    transaction_id: int,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """永久删除回收站中的交易记录"""
    if not current_user or current_user.role != "super_admin":
        raise HTTPException(status_code=403, detail="权限不足：只有超级管理员才能永久删除")
    
    transaction = db.query(Transaction).filter(
        Transaction.id == transaction_id,
        Transaction.is_deleted == 1
    ).first()
    
    if not transaction:
        raise HTTPException(status_code=404, detail="记录不存在")
    
    db.delete(transaction)
    db.commit()
    
    return {"message": "记录已永久删除", "deleted_id": transaction_id}
```

#### 前端 UI

```html
<!-- 回收站页面 -->
<div v-if="activeTab === 'trash'" class="p-6">
    <h2 class="text-lg font-bold text-slate-800 mb-4">🗑️ 回收站</h2>
    
    <!-- 提示信息 -->
    <div class="bg-amber-50 border border-amber-200 rounded-xl p-4 mb-4">
        <p class="text-sm text-amber-800">
            <strong>💡 提示：</strong>已删除的交易记录将在回收站保留 30 天，
            超期将自动永久删除。请及时恢复重要记录。
        </p>
    </div>
    
    <!-- 批量操作 -->
    <div v-if="selectedTrash.length > 0" class="mb-4 flex gap-3">
        <button @click="batchRestoreTrash" class="bg-green-600 text-white px-4 py-2 rounded-lg">
            批量恢复
        </button>
        <button @click="batchPermanentlyDelete" class="bg-red-600 text-white px-4 py-2 rounded-lg">
            永久删除
        </button>
    </div>
    
    <!-- 回收站列表 -->
    <div class="bg-white rounded-xl shadow overflow-hidden">
        <table class="w-full">
            <thead class="bg-slate-50">
                <tr>
                    <th class="text-left py-3 px-4"><input type="checkbox" v-model="selectAllTrash"></th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">删除时间</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">用户</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">产品</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">金额</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">删除人</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">删除原因</th>
                    <th class="text-left py-3 px-4 text-xs font-medium text-slate-500">操作</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="t in trashList" :key="t.id" class="border-t bg-red-50">
                    <td class="py-3 px-4"><input type="checkbox" v-model="selectedTrash" :value="t.id"></td>
                    <td class="py-3 px-4 text-sm">{{ formatDateTime(t.deleted_at) }}</td>
                    <td class="py-3 px-4 text-sm">{{ t.user_name }} ({{ t.department }})</td>
                    <td class="py-3 px-4 text-sm">{{ t.product_name }} × {{ t.quantity }}</td>
                    <td class="py-3 px-4 text-sm font-bold text-blue-600">¥{{ t.actual_price.toFixed(2) }}</td>
                    <td class="py-3 px-4 text-sm">{{ t.deleter_name }}</td>
                    <td class="py-3 px-4 text-sm text-slate-500">{{ t.delete_reason || '-' }}</td>
                    <td class="py-3 px-4">
                        <button @click="restoreOne(t.id)" class="text-green-600 hover:text-green-800 text-sm">恢复</button>
                        <button @click="permanentlyDeleteOne(t.id)" class="text-red-600 hover:text-red-800 text-sm ml-2">永久删除</button>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
```

---

## 二、P1-2: 搜索与筛选增强

### 2.1 高级筛选面板

**需求描述：**
- 支持按用户姓名搜索
- 支持按时间范围筛选
- 支持按产品筛选
- 支持按金额范围筛选
- 支持按部门筛选
- 支持重置筛选条件

**实现方案：**

#### 前端 UI

```html
<!-- 高级筛选按钮 -->
<button @click="showFilterPanel = !showFilterPanel" 
    class="flex items-center gap-2 px-4 py-2 bg-slate-100 rounded-lg hover:bg-slate-200">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
              d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"/>
    </svg>
    高级筛选
</button>

<!-- 高级筛选面板 -->
<div v-if="showFilterPanel" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl p-6 max-w-2xl w-full">
        <h3 class="text-lg font-bold mb-4">🔍 筛选交易记录</h3>
        
        <div class="grid grid-cols-2 gap-4">
            <!-- 用户姓名 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">用户姓名</label>
                <input v-model="filters.userName" type="text" 
                       placeholder="输入姓名或部门"
                       class="w-full px-3 py-2 border border-slate-300 rounded-lg">
            </div>
            
            <!-- 产品名称 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">产品名称</label>
                <select v-model="filters.productName" 
                        class="w-full px-3 py-2 border border-slate-300 rounded-lg">
                    <option value="">全部产品</option>
                    <option v-for="p in products" :key="p.id" :value="p.name">
                        {{ p.name }} ({{ p.specification }})
                    </option>
                </select>
            </div>
            
            <!-- 交易状态 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">交易状态</label>
                <select v-model="filters.status" 
                        class="w-full px-3 py-2 border border-slate-300 rounded-lg">
                    <option value="">全部状态</option>
                    <option value="unsettled">待结算</option>
                    <option value="settled">已结算</option>
                    <option value="reserved">已预定</option>
                </select>
            </div>
            
            <!-- 时间范围 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">时间范围</label>
                <div class="flex gap-2">
                    <input v-model="filters.dateFrom" type="date" 
                           class="flex-1 px-3 py-2 border border-slate-300 rounded-lg">
                    <span class="self-center">至</span>
                    <input v-model="filters.dateTo" type="date" 
                           class="flex-1 px-3 py-2 border border-slate-300 rounded-lg">
                </div>
            </div>
            
            <!-- 金额范围 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">金额范围</label>
                <div class="flex gap-2">
                    <input v-model="filters.amountMin" type="number" placeholder="¥0"
                           class="flex-1 px-3 py-2 border border-slate-300 rounded-lg">
                    <span class="self-center">至</span>
                    <input v-model="filters.amountMax" type="number" placeholder="¥500"
                           class="flex-1 px-3 py-2 border border-slate-300 rounded-lg">
                </div>
            </div>
            
            <!-- 部门筛选 -->
            <div>
                <label class="block text-sm font-medium text-slate-700 mb-1">部门</label>
                <select v-model="filters.department" 
                        class="w-full px-3 py-2 border border-slate-300 rounded-lg">
                    <option value="">全部部门</option>
                    <option v-for="dept in departments" :key="dept" :value="dept">
                        {{ dept }}
                    </option>
                </select>
            </div>
        </div>
        
        <!-- 操作按钮 -->
        <div class="flex gap-3 mt-6">
            <button @click="resetFilters" 
                class="flex-1 px-4 py-2.5 border border-slate-300 rounded-lg font-medium hover:bg-slate-50">
                🔄 重置
            </button>
            <button @click="applyFilters" 
                class="flex-1 px-4 py-2.5 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700">
                🔍 应用筛选
            </button>
        </div>
    </div>
</div>
```

#### 计算属性 - 筛选逻辑

```javascript
const filteredTransactions = computed(() => {
    return transactions.value.filter(t => {
        // 用户姓名筛选
        if (filters.value.userName && 
            !t.user_name.toLowerCase().includes(filters.value.userName.toLowerCase()) &&
            !t.department.toLowerCase().includes(filters.value.userName.toLowerCase())) {
            return false;
        }
        
        // 产品名称筛选
        if (filters.value.productName && t.product_name !== filters.value.productName) {
            return false;
        }
        
        // 状态筛选
        if (filters.value.status && t.status !== filters.value.status) {
            return false;
        }
        
        // 部门筛选
        if (filters.value.department && t.department !== filters.value.department) {
            return false;
        }
        
        // 时间范围筛选
        if (filters.value.dateFrom) {
            const tDate = new Date(t.created_at);
            const fromDate = new Date(filters.value.dateFrom);
            if (tDate < fromDate) return false;
        }
        if (filters.value.dateTo) {
            const tDate = new Date(t.created_at);
            const toDate = new Date(filters.value.dateTo);
            toDate.setHours(23, 59, 59);
            if (tDate > toDate) return false;
        }
        
        // 金额范围筛选
        if (filters.value.amountMin && t.actual_price < filters.value.amountMin) {
            return false;
        }
        if (filters.value.amountMax && t.actual_price > filters.value.amountMax) {
            return false;
        }
        
        return true;
    });
});
```

---

## 三、P1-3: 快捷操作优化

### 3.1 全局快捷键

**需求描述：**
- 支持键盘快捷键操作
- 提供快捷键帮助提示
- 快捷键可配置

**实现方案：**

#### 快捷键映射

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `G + D` | 跳转数据看板 | Go to Dashboard |
| `G + T` | 跳转交易记录 | Go to Transactions |
| `G + I` | 跳转库存管理 | Go to Inventory |
| `/` | 聚焦搜索框 | Focus Search |
| `?` | 显示快捷键帮助 | Show Help |
| `ESC` | 关闭弹窗/菜单 | Close Modal |

#### 前端实现

```javascript
// 快捷键监听
const setupKeyboardShortcuts = () => {
    document.addEventListener('keydown', (e) => {
        // 忽略输入框中的快捷键
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
            return;
        }
        
        // G + X 导航快捷键
        if (e.key === 'g' || e.key === 'G') {
            let nextKey = '';
            const handleNextKey = (event) => {
                nextKey = event.key.toLowerCase();
                if (nextKey === 'd') {
                    activeTab.value = 'dashboard';
                    showToast('已跳转到数据看板', 'success');
                } else if (nextKey === 't') {
                    activeTab.value = 'transactions';
                    showToast('已跳转到交易记录', 'success');
                } else if (nextKey === 'i') {
                    activeTab.value = 'inventory';
                    showToast('已跳转到库存管理', 'success');
                }
                document.removeEventListener('keydown', handleNextKey);
            };
            document.addEventListener('keydown', handleNextKey, { once: true });
        }
        
        // / 聚焦搜索框
        if (e.key === '/' && !e.metaKey && !e.ctrlKey) {
            e.preventDefault();
            focusSearch();
        }
        
        // ? 显示快捷键帮助
        if (e.key === '?' && !e.metaKey && !e.ctrlKey) {
            e.preventDefault();
            showShortcutsHelp.value = true;
        }
        
        // ESC 关闭弹窗
        if (e.key === 'Escape') {
            closeAllModals();
        }
    });
};

// 快捷键帮助弹窗
const showShortcutsModal = () => {
    showShortcutsHelp.value = true;
};
```

#### 快捷键帮助 UI

```html
<!-- 快捷键帮助弹窗 -->
<div v-if="showShortcutsHelp" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl p-6 max-w-lg w-full">
        <div class="flex justify-between items-center mb-4">
            <h3 class="text-lg font-bold">⌨️ 快捷键帮助</h3>
            <button @click="showShortcutsHelp = false" class="text-slate-400 hover:text-slate-600">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
            </button>
        </div>
        
        <div class="space-y-3">
            <div class="flex justify-between items-center">
                <span class="text-slate-600">跳转数据看板</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">G D</kbd>
            </div>
            <div class="flex justify-between items-center">
                <span class="text-slate-600">跳转交易记录</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">G T</kbd>
            </div>
            <div class="flex justify-between items-center">
                <span class="text-slate-600">跳转库存管理</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">G I</kbd>
            </div>
            <div class="flex justify-between items-center">
                <span class="text-slate-600">聚焦搜索框</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">/</kbd>
            </div>
            <div class="flex justify-between items-center">
                <span class="text-slate-600">显示快捷键帮助</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">?</kbd>
            </div>
            <div class="flex justify-between items-center">
                <span class="text-slate-600">关闭弹窗</span>
                <kbd class="px-2 py-1 bg-slate-100 border border-slate-300 rounded text-sm font-mono">ESC</kbd>
            </div>
        </div>
        
        <p class="text-xs text-slate-500 mt-4 text-center">
            💡 提示：在输入框中使用时，快捷键将自动禁用
        </p>
    </div>
</div>
```

### 3.2 快捷操作入口

**需求描述：**
- 首页添加快捷操作卡片
- 一键领水
- 一键结算
- 快速补货

**实现方案：**

```html
<!-- 快捷操作卡片 -->
<div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <button @click="quickPickup" class="bg-white rounded-2xl p-4 card-shadow hover:shadow-lg transition">
        <div class="text-3xl mb-2">💧</div>
        <p class="font-bold text-slate-800">一键领水</p>
        <p class="text-xs text-slate-500 mt-1">领取常用水</p>
    </button>
    
    <button @click="quickSettlement" class="bg-white rounded-2xl p-4 card-shadow hover:shadow-lg transition">
        <div class="text-3xl mb-2">💰</div>
        <p class="font-bold text-slate-800">一键结算</p>
        <p class="text-xs text-slate-500 mt-1">批量结算</p>
    </button>
    
    <button @click="quickRestock" class="bg-white rounded-2xl p-4 card-shadow hover:shadow-lg transition">
        <div class="text-3xl mb-2">📦</div>
        <p class="font-bold text-slate-800">快速补货</p>
        <p class="text-xs text-slate-500 mt-1">库存管理</p>
    </button>
    
    <button @click="showShortcutsHelp = true" class="bg-white rounded-2xl p-4 card-shadow hover:shadow-lg transition">
        <div class="text-3xl mb-2">⌨️</div>
        <p class="font-bold text-slate-800">快捷键</p>
        <p class="text-xs text-slate-500 mt-1">操作指南</p>
    </button>
</div>
```

---

## 四、实施计划

### 第一阶段（1-2 天）
- [ ] 实现结算预览功能
- [ ] 实现回收站后端 API
- [ ] 实现回收站前端页面

### 第二阶段（1-2 天）
- [ ] 实现高级筛选面板
- [ ] 实现筛选计算属性
- [ ] 测试筛选功能

### 第三阶段（1 天）
- [ ] 实现全局快捷键
- [ ] 实现快捷键帮助弹窗
- [ ] 实现快捷操作入口

### 测试与优化（1 天）
- [ ] 功能测试
- [ ] 性能优化
- [ ] 用户体验优化

---

## 五、预期收益

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 批量结算效率 | 手动逐条 | 预览后批量 | ↑200% |
| 误删恢复时间 | 无法恢复 | 回收站恢复 | ↑100% |
| 搜索效率 | 基础筛选 | 多维度筛选 | ↑150% |
| 操作便捷性 | 鼠标操作 | 键盘快捷键 | ↑100% |

---

**规划完成日期：** 2026-03-24  
**实施负责人：** AI 产品经理专家
