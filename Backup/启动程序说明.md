# 快捷启动程序使用说明

## 文件列表

启动器包含以下文件：

| 文件名 | 说明 | 适用平台 |
|--------|------|---------|
| `start_services.py` | Python启动脚本（主程序） | Windows/Mac/Linux |
| `start_services.bat` | Windows批处理启动脚本 | Windows |
| `start_services.sh` | Linux/Mac Shell启动脚本 | Linux/Mac |
| `start_services_example.py` | 使用示例代码 | Windows/Mac/Linux |
| `快速启动指南.md` | 详细使用文档 | - |

## 快速开始

### Windows用户
双击 `start_services.bat` 即可

### Mac/Linux用户
运行：
```bash
./start_services.sh
```

### 通用方法
```bash
python start_services.py
```

## 自动功能

✅ **自动端口检测** - 自动选择可用端口，避免端口冲突
✅ **自动启动服务** - 依次启动水站、会议室、用餐三个服务
✅ **自动打开浏览器** - 启动成功后自动打开系统首页
✅ **自动日志记录** - 所有服务日志统一保存到logs目录
✅ **一键停止** - Ctrl+C停止所有服务

## 启动流程

1. **检查端口占用** - 扫描8000-8099端口，自动分配
2. **启动水站服务** - 端口8000（默认）
3. **启动会议室服务** - 端口8001（默认）
4. **启动用餐服务** - 端口8002（默认）
5. **打开浏览器** - 自动打开Portal首页
6. **显示访问地址** - 显示所有服务的访问链接

## 访问地址

启动后会显示以下访问地址：

```
🏠 首页: http://localhost:8000/portal/index.html
💧 水站管理: http://localhost:8000/water-admin/index.html
🏢 会议室: http://localhost:8000/meeting-frontend/index.html
🍽️ 用餐管理: http://localhost:8000/portal/admin/dining.html
📊 API文档: http://localhost:8000/docs
```

## 端口分配

如果默认端口被占用，启动器会自动递增到下一个可用端口：

| 服务 | 默认端口 | 实际端口 | 说明 |
|------|---------|---------|------|
| 水站服务 | 8000 | 8000+ | 主服务，包含Portal |
| 会议室服务 | 8001 | 8001+ | 独立服务 |
| 用餐服务 | 8002 | 8002+ | 独立服务 |

## 日志文件

日志保存在 `logs/` 目录：

```
logs/
├── water_service.log       # 水站服务日志
├── meeting_service.log     # 会议室服务日志
└── dining_service.log      # 用餐服务日志
```

## 故障排查

### 端口被占用
**现象**: 提示端口被占用
**解决**: 启动器会自动选择其他端口，无需手动处理

### 服务启动失败
**现象**: 提示服务启动超时或失败
**解决**: 
1. 检查日志文件：`logs/*.log`
2. 确认Python版本：需要Python 3.8+
3. 确认依赖已安装：`pip install -r requirements.txt`

### 浏览器未打开
**现象**: 服务启动成功但浏览器未自动打开
**解决**: 手动在浏览器中访问显示的首页地址

### Python未安装
**现象**: 提示找不到Python
**解决**: 从 https://python.org 下载并安装Python 3.8+

## 高级配置

### 修改默认端口
编辑 `start_services.py`，修改 `start_port` 参数：
```python
water_port = find_available_port(9000)  # 从9000开始
meeting_port = find_available_port(9001)
dining_port = find_available_port(9002)
```

### 禁用自动打开浏览器
编辑 `start_services.py`，注释或删除：
```python
# webbrowser.open(primary_url)
```

### 修改服务配置
编辑各服务的配置文件：
- `Service_WaterManage/backend/config/settings.py`
- `Service_MeetingRoom/backend/config/settings.py`
- `Service_Dining/backend/config/settings.py`

## 代码示例

查看 `start_services_example.py` 了解如何：
- 启动单个服务
- 检查端口占用
- 停止服务

## 生产环境部署

生产环境建议：
1. 使用Gunicorn或uWSGI运行服务
2. 配置Nginx反向代理
3. 使用systemd管理服务
4. 配置SSL证书

详见 `云服务器部署配置说明.md`

## 技术支持

如有问题，请：
1. 查看日志文件：`logs/*.log`
2. 查看 `快速启动指南.md`
3. 查看 `云服务器部署配置说明.md`

## 更新日志

### v1.0 (2026-04-06)
- ✅ 初始版本发布
- ✅ 自动端口检测
- ✅ 自动启动服务
- ✅ 自动打开浏览器
- ✅ 统一日志管理
- ✅ 一键停止所有服务